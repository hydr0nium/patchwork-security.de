# The URL the site will be built for
base_url = "https://patchwork-security.de"
theme = "terminus" 
title = "Patchwork Security"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

author = "sol"

taxonomies = [
    { name = "tags", feed = true },
    { name = "categories", feed = true, paginate_by = 5 },
]

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
render_emoji = true
bottom_footnotes = true
highlight_theme = "monokai"

[extra]
# Put all your custom variables here
favicon_emoji = "💾"
copy_button = true
layout = "center"



main_menu = [
    { name = "home", url = "/", trailing_slash = true },
    { name = "tags", url = "tags", trailing_slash = true },
    { name = "ctf", url = "ctf", trailing_slash = true },
    { name = "github", url = "https://github.com/hydr0nium", new_tab = true },
]

socials = [
    { name = "github", url = "https://github.com/hydr0nium" },
    { name = "mastodon", url = "https://infosec.exchange/@hydr0nium" }
]

[extra.responsive_images]
widths = [640, 784, 1280, 1920, 2560]
fallback_width = 1280
